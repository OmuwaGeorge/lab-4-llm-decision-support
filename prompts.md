Prompt Evolution: Summary prompt had 2 versions, the first was inconsistent in length and missed key facts. The second enforced explicit constraints which made the summaries more consistent and factual. Extract Prompt made use of an example to avoid leaking answers and enforced "not null" to stop the model from inferring numbers. Brief prompt enforced the model not to approve or reject outputs, ensuring a human makes the final decision.
